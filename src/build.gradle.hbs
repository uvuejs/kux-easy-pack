// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
  //   repositories {
		// {{#if project.repositories.mavenCentral}}
		// mavenCentral()
		// {{/if}}
  //       {{#if project.repositories.jcenter}}
  //       jcenter()
  //       {{/if}}
  //       {{#if project.repositories.google}}
  //       google()
  //       {{/if}}
  //       {{#each project.repositories.maven}}
  //       maven { url '{{this}}' }
  //       {{/each}}
  //   }
    dependencies {
        {{#each project.dependencies.files}}
        classpath(files('{{this}}'))
        {{/each}}
		{{#each project.dependencies.default}}
		classpath '{{this}}'
		{{/each}}
    }
}
plugins {
    id 'com.android.application' version '8.2.2' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.22' apply false
    id 'com.android.library' version '8.2.2' apply false
    {{#each project.plugins}}
    id '{{this}}'
    {{/each}}
}
allprojects {
  //   repositories {
		// {{#if repositories.mavenCentral}}
		// mavenCentral()
		// {{/if}}
  //       {{#if repositories.jcenter}}
  //       jcenter()
  //       {{/if}}
  //       {{#if repositories.google}}
  //       google()
  //       {{/if}}
  //       {{#each repositories.maven}}
  //       maven { url '{{this}}' }
  //       {{/each}}
  //   }
}